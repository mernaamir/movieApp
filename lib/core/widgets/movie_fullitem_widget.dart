import 'package:flutter/material.dart';

class MovieFullItemWidget extends StatelessWidget {
  const MovieFullItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
